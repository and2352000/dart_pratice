main() {
  // bool? isFinished;
  // isFinished = true;
  // isFinished = isFinished == null ? true : false;
  // isFinished = isFinished ?? true;
  // print(isFinished);

  // StringBuffer sb = StringBuffer();
  // sb.write('aaa');
  // sb
  //   ..write('bbb')
  //   ..write('ccc');
  // print(sb);

  String? a;
  print(a?.length);
}
